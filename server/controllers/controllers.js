const {v4: uuid} = require("uuid");const {users, movies} = require('../dataBase/data')module.exports = {    register: (req, res) => {        const {user, passOne} = req.body        const id = uuid()        users.push({user, password: passOne, id})        res.send({success: true, message: "Registered"})    },    logUser: (req, res) => {        const {user, pass} = req.body        const isUser = users.find(x => x.user === user && x.password === pass)        isUser ? res.send({success: true, message: {user: user, id: isUser.id}}) : res.send({            success: false,            message: "No such user"        })    },    createItem: (req, res) => {        res.send({success: true, message: movies})    },    comment: (req, res) => {        const {id, comment, creator} = req.body        const user = users.find(x => x.id === creator)        const movie = movies.find(x => x.id === id)        if (!(!!movies)) return res.send({success: false})        comment.user = user.user        movie.reviews.unshift(comment)        res.send({success: true, message: movies})    }}