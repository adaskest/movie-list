const {v4: uuid} = require("uuid");let {users, movies, notifications} = require('../dataBase/data')module.exports = {    register: (req, res) => {        const {user, passOne} = req.body        const id = uuid()        users.push({user, password: passOne, id, lastCon: 0})        res.send({success: true, message: "Registered"})    },    logUser: (req, res) => {        const {user, pass, time} = req.body        const isUser = users.find(x => x.user === user && x.password === pass)        isUser.lastCon = time        res.send({success: true, message: {user: user, id: isUser.id, notifications}})    },    logout: (req, res) => {        notifications.splice(0, notifications.length)        res.send({success: true})    },    createItem: (req, res) => {        res.send({success: true, message: movies})    },    comment: (req, res) => {        const {movieId, comment, creator} = req.body        const user = users.find(x => x.id === creator)        const movie = movies.find(x => x.id === movieId)        comment.user = user.user        movie.commentTime = comment.time        movie.reviews.unshift(comment)        res.send({success: true, message: movies})    },    getInfo: (req, res) => {        res.send({success: true, message: movies})    }}