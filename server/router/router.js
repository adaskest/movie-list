const express = require('express')const router = express.Router()const {register, logUser, createItem,comment} = require('../controllers/controllers')const {registration, fetchData, checkUser} = require('../middle/validator')router.post('/register', registration, register)router.post('/login', checkUser, logUser)router.post('/create', fetchData, createItem)router.post('/comment', comment)module.exports = router