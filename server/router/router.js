const express = require('express')const router = express.Router()const {register, logUser, createItem, comment, getInfo, logout, removeNotif} = require('../controllers/controllers')const {registration, fetchData, checkUser, checkComments} = require('../middle/validator')router.post('/register', registration, register)router.post('/login', checkUser, logUser)router.get('/logout/:id', logout)router.post('/create', fetchData, createItem)router.post('/comment', checkComments, comment)router.get('/get-info', getInfo)router.post('/remove-notification', removeNotif)module.exports = router