import {createSlice} from "@reduxjs/toolkit";export const moviesSlice = createSlice({    name: 'movies',    initialState: {        value: {            movies: [],        }    },    reducers: {        addMovies: ({value}, {payload}) => {            value.movies = payload        },    }})export const {addMovies, addNotif} = moviesSlice.actionsexport default moviesSlice.reducer