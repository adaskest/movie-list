import React from 'react';import {Card, CardActionArea, CardContent, CardMedia, Typography} from "@mui/material";import {useNavigate} from "react-router-dom";const Movie = ({movie}) => {    const nav = useNavigate()    function goTo() {        nav('/movie/' + movie.id)    }    return (        <Card onClick={goTo} sx={{width: 300, height: 500, m: 2,}}>            <CardActionArea>            <CardMedia                style={{objectFit: "fill"}}                component="img"                height="350"                image={movie.poster}                alt=""            />            <CardContent>                <Typography gutterBottom variant="h5" component="div">                    Title: {movie.title}                </Typography>                <Typography variant="body2" color="text.secondary">                    Year: {movie.year}, Rating: {movie.rating}                </Typography>                <Typography variant="body2" color="text.secondary">                    Genres: {movie.genres}                </Typography>                {movie.writers && <Typography variant="body2" color="text.secondary">                    Writers: {movie.writers}                </Typography>}                <Typography variant="body2" color="text.secondary">                    <b>{movie.createdBy}</b>                </Typography>            </CardContent>            </CardActionArea>        </Card>    );};export default Movie;